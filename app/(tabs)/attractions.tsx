import React from "react";
import SwipingScreenLayout from "@/components/layouts/SwipingScreenLayout";

const placeType = "tourist_attraction";

const Attractions = () => {
    return <SwipingScreenLayout placeType={placeType} />;
}

export default Attractions;